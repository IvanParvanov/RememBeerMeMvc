using System.Web.Mvc;

using Moq;

using NUnit.Framework;

namespace RememBeer.Tests.Mvc.Helpers.AjaxHelperExtensionsTests
{
    [TestFixture]
    public class PageRefreshActionLink_Should
    {
        //[TestCase("text", "class", "Index", "Home")]
        //public void ReturnCorrectString(string linkText, string className, string action, string controller)
        //{
        //    var helper = new AjaxHelper(new Mock<ViewContext>().Object, new Mock<IViewDataContainer>().Object);
        //    var result = helper.PageRefreshActionLink(linkText, className, action, controller, null);

        //    var actual = result.ToString();

        //    StringAssert.Contains($" class=\"{className}\" ", actual);
        //    StringAssert.Contains($" href=\"{controller}/{action}\" ", actual);
        //    StringAssert.Contains($" >{linkText}</a>", actual);
        //}
    }
}
